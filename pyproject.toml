[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "fake-useragent-min"
version = "0.1.0"
requires-python = ">=3.9"
description = "A minimal project demonstrating user-agent utilities and ruff-based tooling."
dependencies = []

[project.optional-dependencies]
dev = ["pytest", "ruff"]

[tool.ruff]
target-version = "py39"
# Initial, intentionally minimal configuration to be enhanced.

